<?php

    // ------------------- SUMA -----------------------------
    function suma ($p, $q, $pe){
        $suma = ($p + $q + $pe);
        echo "La suma es: ", $suma, "</br>";
    }

    // ------------------- OPUESTO ----------------------------------
    function negativo ($num){
        $negNum = ($num * -1);
        return $negNum;
    }
